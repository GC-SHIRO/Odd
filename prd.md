项目名： Odd

我创建这个项目的目的是从头学习构建Agent，做一个能够自主进行
- 在workspace操作，能使用命令行
- 能够使用mcp和anthropic定义的skill
- 能够召唤子agent协助自己工作
- 能够给自己制定计划并执行
- 能够进行上下文压缩和管理

使用python开发，从模型的调用开始 **逐步** 完成开发

# 要求
代码结构必须规范，不能大规模把大量逻辑堆在一个代码里并且符合标准的开发规范
最小原则，只要可实现就不要多余代码

# 阶段
开发按照一个个阶段完成，不要一次性完成大量的工作，先计划出方案